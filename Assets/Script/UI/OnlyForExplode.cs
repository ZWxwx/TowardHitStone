
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class OnlyForExplode : MonoBehaviour
{
    // Start is called before the first frame update
    void end()
    {
        Destroy(gameObject);
    }
}
